import { SetAmountColorPipe } from "./set-amount-color.pipe";

describe("SetAmountColorPipe", () => {
	it("create an instance", () => {
		const pipe = new SetAmountColorPipe();
		expect(pipe).toBeTruthy();
	});
});
